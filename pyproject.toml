[project]
name = "gennady-i2v"
version = "0.1.0"
description = "Modular scene-graph driven image-to-video engine prototype"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
